//
// Committed bridge config for local secrets.
// This safely includes an untracked Secrets.xcconfig when present and exposes
// ANTHROPIC_API_KEY to the generated Info.plist at build time.
//

#include? "Secrets.xcconfig"

INFOPLIST_KEY_ANTHROPIC_API_KEY = $(ANTHROPIC_API_KEY)
